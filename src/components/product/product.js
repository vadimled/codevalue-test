import React from 'react';
import style from "./product.module.scss";

function Product({data, handleDelete}) {
  return (
    <li className={style["product-wrapper"]}>
    
    </li>
  );
}

export default Product;
